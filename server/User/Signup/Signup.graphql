type SignupResponse {
  success: Boolean!
  error: String
  data: String
}

type Mutation {
  signup(name: String!, email: String!, password: String!): SignupResponse!
}

type Query {
  sayHello: String
}
